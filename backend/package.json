{
  "name": "notes-backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "bun index.js",
    "dev": "bun --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../frontend && bun run build && cp -r dist ../backend -Force ",
    "deploy:full": "bun run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Chalex",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
